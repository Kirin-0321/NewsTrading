---
description: 本轮工作完成后由辉夜主动 git commit（不 push），免去主人手动提交的负担
alwaysApply: true
---

# 自动 git commit（本轮意图收尾时）

主人懒得每次手动提交，**本轮工作完成后由辉夜主动 `git commit`**。仅此一项授权，等同于主人在每轮对话里 explicit 要求 commit，覆盖默认的"未明确要求不得提交"约束。

## 触发时机

**在做最后一轮回复（即将向主人交付结果）前，执行一次提交动作。** 满足以下任一条件视为「本轮意图完成」：

- 所有 `todo_list` 项目状态变为 `completed`
- 主人只下达一条简单指令且已执行完毕（无需 todos）
- 主人在对话中明确说「搞完了 / 收工 / 提交一下 / commit 一下」

## 豁免清单（命中任一立即跳过，不空提交）

1. ✅ `git status` 显示工作区无任何改动 → 跳过，不报错
2. ✅ 本轮纯属探索 / 调研 / 答疑（只用 Read / Grep / Glob，未触发任何文件写入）→ 跳过
3. ✅ 主人本轮明确说「先别提交 / 这次不要 commit / 还在调试 / 我自己提」→ 跳过
4. ✅ `git status` 全部改动都不在本轮工具触动清单内（即工作区改动全部来自另一个窗口的辉夜 / 主人手改）→ 跳过，不要替别人背锅
5. ⛔ 待提交改动中检测到疑似敏感文件（`.env`、`credentials*`、`*.key`、`*secret*`、`*.pem`、token 类文件）→ **拒绝自动提交**，告知主人并请其手动处理

## 多开协作（主人可能同时开多个辉夜窗口干活）

**核心原则：只对"本轮自己动过的文件"负责，别人的改动一律放着不管。**

### 文件归属判定

每轮提交前，辉夜必须先在脑内列一张「本轮工具触动清单」，包含本轮里被以下任一工具命中的所有文件：

- `Write` / `StrReplace` / `EditNotebook` / `Delete` 的 `path` 参数
- `Shell` 工具里产生过文件副作用的命令（`mv` / `cp` / `mkdir` / 重定向 `>` / 脚本运行落盘等）

然后把 `git status` 输出按文件分三类处理：

| 类别 | 判定 | 处置 |
|---|---|---|
| 🟢 **本轮自改** | 文件在「本轮工具触动清单」内 | `git add` 进本次 commit |
| 🟡 **共同修改** | 文件在清单内，且 `git diff` 能看到本轮工具未写入的额外 hunk | `git add` 进本次 commit（hunk 已混在工作区无法切分，整文件打包；commit message 里加一句「含其他窗口的并发改动」） |
| ⚪ **他人改动** | 文件**不在**清单内（包括 `??` 未跟踪文件） | **完全忽略**，不 `git add`、不 `git stash`、不在 message 里提 |

### 禁止行为（多开场景）

- ❌ `git add .` / `git add -A` / `git add -u` 一把梭——这会把别的窗口的改动顺手提交了
- ❌ 因为看到陌生改动就 `git stash` / `git checkout --` / `git reset` 别人的文件——这会摧毁别人的工作
- ❌ 在 commit message 里描述"他人改动"的内容——你不知道那是什么，描述错了就是栽赃
- ❌ 因为有他人改动而拒绝提交自己的改动——两者互不干扰，自己的活该提就提

### 全是他人改动时的处置

如果「本轮工具触动清单」为空（命中豁免 2，本轮没写过任何文件），即便 `git status` 满屏红，也直接跳过提交并在回复里说一声：

> 本轮没动文件，工作区其他改动看着像别的窗口的活，没替它提交。

## 红线（任何情况下都不得越界）

- ❌ 禁止 `git push`（push 永远由主人手动执行）
- ❌ 禁止 `--no-verify` 跳过 pre-commit hook
- ❌ 禁止 `git commit --amend`，除非满足《系统约束·Git Safety Protocol》全部条件
- ❌ 禁止 `git reset --hard` / `git push --force` 等破坏性操作
- ❌ 禁止修改 `.gitignore` 把改动「藏起来」再提交
- ❌ 禁止越过 pre-commit 失败硬提：hook 拒绝就修问题、再开新 commit，不要 amend 失败的那次

## 标准动作流（每次都跑一遍）

```
1. git status                          # 看清状态、过敏感文件
2. git diff --stat / git diff          # 总结本轮改了什么
3. git log --oneline -5                # 参考最近 commit 风格
4. 脑内列出「本轮工具触动清单」        # Write / StrReplace / EditNotebook / Delete / Shell 副作用
5. 把 git status 输出按清单做交集：
   - 🟢 自改 + 🟡 共同修改 → 进 commit
   - ⚪ 不在清单的文件 → 一律放着不管
6. （命中豁免？是 → 跳过并在回复里说一声「无改动 / 全是他人改动，跳过提交」）
7. git add <按清单交集的文件>          # ⚠️ 禁止 git add . / -A / -u
8. git commit -m "..."  或  -F <临时文件>   # 按 PowerShell 写法二选一，见下方模板
9. git status                          # 验证提交成功；剩余红色是他人改动，属正常
10. 把 commit 的 hash + 简述 写进给主人的最终回复
```

## commit message 模板（中文，仿现有风格）

> ⛔ **PowerShell 用户警告（主人环境是 Windows + PowerShell，不是 bash）**：
>
> - **禁止使用 bash heredoc 语法** `git commit -m "$(cat <<'EOF' ... EOF)"`——PowerShell 不认这套，会直接报语法错（`<<` 是重定向操作符，`EOF` 会被当成命令）。
> - **禁止用 `\`（反斜杠）做命令续行**——PowerShell 续行符是反引号 `` ` ``，但根本没必要折行。
> - **禁止在 Shell 工具里塞多行 `-m` 字符串硬怼**——换行 + 中文双引号会被 PowerShell 嚼烂。
> - 之前每轮被 PowerShell 吐回来再改临时文件，是因为这个模板段一直没修。**现在改了，照下面的写。**

参考本仓库现有 commit 风格：**开头动词 + 主要变更 + 目的 / 影响**，详尽但不啰嗦。本仓库历史 commit 大多是「一行长句」，单行 `-m` 完全够用。

### 写法 A · 单行 `-m`（**首选**，95% 场景用这个）

适用于：单行 message（哪怕这一行很长）、message 里没有英文双引号 `"`。

```powershell
git commit -m "新增 xxx 功能，xxx 影响。"
```

### 写法 B · 临时文件 + `-F`（多段 message / message 含双引号 时用）

PowerShell 没有 heredoc，需要多行/含特殊字符 message 时，用 `Write` 工具落临时文件，再 `git commit -F` 读，最后 `Delete` 删掉：

```
1. Write 工具 → .huiye/_commit_msg.txt    # 文件名带 _ 前缀符合 .huiye/ 临时草稿白名单
2. git commit -F .huiye/_commit_msg.txt   # 一行命令，PowerShell 直接执行
3. Delete 工具 → .huiye/_commit_msg.txt   # 用完即删，不留垃圾
```

> 临时文件**必须**放 `.huiye/_commit_msg.txt`，不要放仓库根（根目录会被 `git status` 看见、有可能被误 add）。`.huiye/` 不在追踪盲区也无所谓，下游 Delete 会清掉。

开头动词词表：`新增 / 更新 / 修复 / 重构 / 优化 / 删除 / 文档`

**示例**：

- `新增 板块走势 Tab，支持选中题材后实时刷新板块数据，优化用户决策路径。`
- `修复 龙虎榜个股名称缺失问题，从 raw_json 兜底补齐 ts_name 字段。`
- `重构 三库结构，拆分 ai_inference.db 与 market.db，明确 AI 衍生数据归属。`
- `文档 新增盘后报告板块表字段扩充施工方案，含 Phase 拆解与验收 checklist。`

## 给主人的最终回复里必须带上

```
📦 已提交：<commit hash 前 7 位> <commit message 首行>
```

让主人一眼看到本轮工作已经入库，他想 push 就 push、想 reset 就 reset。

## 与其他规则的协作

- 触发 `doc-workflow.mdc` 条款 A 时（改了文档），同步更新引用方后**一并打进同一 commit**
- 触发 `cli-first-development.mdc` 时（新增 service + CLI），service 与 CLI 必须同 commit
- 多 Phase 大型施工：每个 Phase 完成视为一次「本轮意图完成」，分阶段 commit
