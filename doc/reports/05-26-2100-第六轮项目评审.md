# 项目代码 Review 报告（第六轮）

> 评审者: 辉夜  
> 评审时间: 2026-05-26 21:00  
> 项目: A股新闻爬虫与 AI 分析系统 (`F:\NewsTrading`)  
> 评审范围: `.huiye/` 文档全量 + `core/` + `services/` + `gui/` + `prompts/` + `config/` + `agent/` + `tools/` + `README.md`  
> 触发: 主人「重新 review 项目」  
> 关联: 上一轮见 [`05-23-1800-第五轮代码评审-OBSOLETE.md`](./05-23-1800-第五轮代码评审-OBSOLETE.md)（OBSOLETE，写于 2026-05-23）

---

## 0. TL;DR — 本轮基调

**「文档写完了，代码做一半」**。施工方案的 §18.1 / §20 把 Phase M0 验收勾得满满当当，但实际进 `prompts/` 一看：
- ❌ `_shared/` 目录根本不存在
- ❌ 4 个 prompt 文件没按 §4.7 重命名
- ❌ 3 个 `_index.json` 一个都没建

PromptLoader 的 `{{include:xxx}}` 机制写得很完整，但**全项目 `prompts/**/*.md` 零命中**——这意味着所谓的「文件复用消除 30% 重复」**纯口头**。

其他 12 项是程度较轻的 docstring / config / README 不一致。

| 等级 | 数量 | 主线 |
|:---:|:---:|---|
| 🔴 严重 | 3 | Prompt 文件化是半成品 |
| 🟠 高危 | 4 | API Key 明文 + 配置与文档矛盾 |
| 🟡 中危 | 6 | README / docstring 老旧 |
| 🟢 低危 | 2 | doc/ 目录基本空、归档待清理 |

---

## 1. 🔴 严重问题（"已完成"是假的）

### S1. `prompts/_shared/` 目录**根本不存在**

**文档承诺**（[`盘后数据自动化施工方案`](../design/05-26-2126-盘后数据自动化施工方案.md) §4.4 / §14.1）:
```
prompts/_shared/
├── news_priority_rules.md      被 8 个模板引用
├── one_day_trip_warning.md     被 2 个引用
├── high_position_warning.md    被 2 个引用
├── sentiment_cycle.md          被 5 个引用
├── news_link_format.md         被 10 个引用
└── README.md
```

**验收声明**（§18.1）:
- [x] `prompts/analysis/*.md` 全部含 frontmatter + SYSTEM + USER 三段
- [x] `PromptLoader.validate_all()` 返回 []

**真实情况**:
- `prompts/` 下只有 `analysis/`、`market_fetch/`、`theme_extraction/` 三个目录
- 全项目 `grep "\{\{include:"` 在 `prompts/**` 命中 **0 个文件**
- `core/prompt_loader.py:506-558` 的 `_expand_includes` / `_load_shared` 写得很完整，但被调用 0 次

**影响**: §4.4 表中声称的「复用次数 8 / 10 / 5」全是空头支票，所谓「主人改一处全部生效」根本没法发生。

**主人决策**（2026-05-26）: **不补做**——文档改为"该方案未实施"。

---

### S2. Prompt 文件重命名**未发生**

**文档承诺**（§4.7 / §14.1）:

| 原 ID | 应改为 |
|------|------|
| custom_6 | news_focused |
| custom_7 | super_comprehensive |
| custom_1769178949 | trinity_resonance |
| custom_1770292858 | trinity_v2_0205 |

**真实情况**:
- `prompts/analysis/` 仍然是 `custom_6.md` / `custom_7.md` / `custom_1769178949.md` / `custom_1770292858.md`
- `config/ai_config.json` 中 `current_prompt_template: "custom_6"`、`current_template: "custom_6"`

**主人决策**（2026-05-26）: **保持现状**——文档改为"不重命名，保留历史 ID"。

---

### S3. 每类别 `_index.json` 一个都没有

**文档承诺**（§4.2 / §14.1）:
```
prompts/analysis/_index.json
prompts/market_fetch/_index.json
prompts/theme_extraction/_index.json
```

**真实情况**: 三个都不存在；`PromptLoader.list_category()` 直接 `glob("*.md")` 跳过 `_` 开头的文件，**根本不需要 `_index.json`**。

**主人决策**: **不补做**——文档改为"未实施，PromptLoader 已通过文件扫描替代"。

---

## 2. 🟠 高危：配置 / 安全 / 与文档不一致

### H1. API Key 明文写在 `config/ai_config.json` 里

```12:39:config/ai_config.json
    "deepseek": { "api_key": "sk-6cfe995c...", ... },
    "qwen":     { "api_key": "sk-92788988...", ... },
    "volcengine": { "api_key": "029b7ee4...", ... }
```

**风险**:
- 虽然 `.gitignore` 排除了 `config/ai_config.json`，但本地副本仍是定时炸弹
- 一旦 `git add -f` / 打包到 EXE 即泄密
- 项目设计本就推荐 `.env`，`ai_config.json` 这里 api_key 字段应为空字符串

**建议**: 把三家 Key 迁到 `.env`，`ai_config.json` 中 `api_key` 全部清空。

---

### H2. `ai_config.json` 缺 `market_fetch` 段

**文档**（§4.8 / README §"盘后数据 §配置"）说应有完整 `market_fetch` 段。

**真实**: `config/ai_config.json` 完全没有这段，靠 `core/ai_config.py::get_market_fetch_config()` 内置默认值兜底。

**影响**: 能跑，但主人想调 `enable_sectors / enable_traders / max_tokens` 时无可见配置入口，必须改代码。

---

### H3. `theme_extraction.model` 与「全链路 v4-pro」决策矛盾

[`NewsTrading架构方案`](../design/05-26-2126-NewsTrading架构方案.md) §13 / §14.5 明文「全链路统一 deepseek-v4-pro」。

实际 `config/ai_config.json::theme_extraction.model = "deepseek-v4-flash"`。

**两种解释**:
1. 主人后续又改回了 flash 求成本（但没更新文档）
2. M0 期间漏改

需要主人确认意图。

---

### H4. `current_prompt_template` / `current_template` 双字段污染

```3:56:config/ai_config.json
  "current_prompt_template": "custom_6",
  ...
  "current_template": "custom_6",
```

同一意图两份冗余；文档施工方案 §13.3 说要改成 `current_prompt: {category, id}`，**未改**。

---

## 3. 🟡 中危：docstring / README 老旧

### M1. `agent/__init__.py` docstring 写「Phase 6 占位」

```1:9:agent/__init__.py
"""
Agent MCP 入口占位（Phase 6）
后续在此注册 tools，映射到 services 层:
  - crawl_news  -> services.crawl_sync_service.crawl_sync
  ...
"""
```

实际下面 4 个函数已经导出可用（`crawl_sync / clean_sync / analyze_news / get_db_stats`），不再是「占位」。

---

### M2. README 项目结构图漏 `prompts/` 与 `services/market/`

`README.md` 第 287~324 行的目录树**完全没有**：
- `prompts/` （M0 之后的核心目录）
- `services/market/` （M1~M5 14 张表）
- `data/market.db`

而且 `core/` 还在列老的 `news_crawler_scroll.py / news_cleaner.py / ai_news_analyzer.py / theme_extractor.py / semantic_dedup.py / scheduler_service.py / ai_config.py`，没有 `prompt_loader.py`。

---

### M3. README 导航表漏「📝 提示词管理」

`README.md` 第 99~108 行的页面表只列 8 个页面；`gui/main_window.py:135` 已经多了 `('prompt_manager', '📝 提示词管理')`。

---

### M4. README §"AI 配置" 描述过期

```261:268:README.md
- `prompt_templates` — 标准 / 激进 / 稳健 / 价值 / 短线 五套分析模板
```

实际：
1. `ai_config.json` 早就**不再有 `prompt_templates` 字段**（M0 已迁出）
2. 实际不止 5 套，是 10 套（M0 迁出后保留全部，含 4 个 custom_*）

---

### M5. `gui/main_window.py:129` 死注释行

```129:129:gui/main_window.py
#            ('analysis', '📊 新闻分析（弃用）'),
```

弃用就直接删，保留注释只制造疑惑。

---

### M6. `ai_config.example.json` 与瘦身后的 `ai_config.json` 体积不一致

`ai_config.example.json` 15K，`ai_config.json` 1.6K。example 里仍是 M0 前的完整 `prompt_templates`，需不需要同步瘦身？

---

## 4. 🟢 低危：文档分类与归档

### L1. `doc/` 目录基本是空的

文档分类规范定义 6 大类，但 `doc/` 下只有 `doc/reports/05-23-2100-Tushare题材数据源对比.md` 和 `doc/其他/清洗规则.md`。

项目所有核心架构文档全部堆在 `.huiye/`——这是辉夜的工作目录，不该作为对外公开文档源。

**已落实**: [CLI使用文档](../guides/05-26-2126-CLI使用文档.md) / [盘后数据自动化功能说明](../features/05-26-2126-盘后数据自动化功能说明.md) 已迁入 `doc/guides/` 与 `doc/features/`（详见 `doc/updates/05-26-2126-工作目录文档迁移.md`）。

---

### L2. `.huiye/_post_migration_sample.md` 已 archive 但仍在主目录

`.huiye/README.md` 标注为 `⚪ ARCHIVE`，可挪到 `.huiye/_archive/` 子目录整理。

---

## 5. 已确认 OK 的部分

| 子系统 | 状态 |
|--------|------|
| `services/market/` 14 个文件 / 14 张表 | ✅ 完整、与施工方案一致 |
| `services/storage/` v2 单表 (`raw_news.clean_status`) | ✅ 无残留 `curated_store` |
| `core/prompt_loader.py` | ✅ 代码完整，frontmatter / SYSTEM / USER / include 全部实现 |
| `MarketSummaryService.build` 链路 | ✅ M5 实测 30 天回填 100% 成功，平均完整度 95.24% |
| `data/news.db.ai_reports` 索引表 | ✅ M4.5 已实装 |
| `gui/pages/market_summary_page.py` + 7 个 Tab | ✅ M3.13 验收通过 |
| `tools/test_market_db.py` + `tools/test_prompt_loader.py` + `tools/test_phase_m0_acceptance.py` | ✅ 全 PASS |
| `gui/main_window.py` 导航 9 项 | ✅ 含 `prompt_manager / market_summary` |
| `agent/__init__.py` 4 个函数导出 | ✅ 实际可用 |

---

## 6. 处置决策（2026-05-26 主人裁定）

| 议题 | 决策 |
|------|------|
| 🔴 三项严重问题 | **不补做实际代码**，文档改为"未实施" |
| 🟠 H1 API Key | 待主人下一轮处理 |
| 🟠 H2 H3 H4 配置一致性 | 待主人下一轮处理 |
| 🟡 M1~M6 README/docstring | 待主人下一轮处理 |
| 🟢 L1 L2 文档归档 | 待主人下一轮处理 |

**本轮只做**: 修文档（施工方案 + `.huiye/README.md` + 架构方案 §15.3）让其与实际代码一致。

---

## 7. 给主人的话

呜……人家不想说重话，但这份施工方案 §18.1 / §20 的"验收 ☑"打得人家直翻白眼。验收勾选是给未来的辉夜自己看的——若每次开下一个 Phase 都要重新查一遍代码才能确认上个 Phase 是不是真的完成，那写文档干嘛？

下次施工，**验收 ☑ 必须配以"验证命令 / 验证输出"**——比如 §18.1 应该写：

```
- [x] prompts/_shared/*.md 存在
      验证: ls prompts/_shared/   → 5 个 .md
      实测: 2026-05-26 21:00 跑通
```

而不是只打个勾就走。
